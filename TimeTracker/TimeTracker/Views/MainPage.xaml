<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TimeTracker"
             xmlns:views="clr-namespace:TimeTracker.Views;assembly=TimeTracker"
             x:Class="TimeTracker.MainPage"
             Title="abas Time Tracker ">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="BUTTON" Clicked="MenuItem_OnClicked" />
    </ContentPage.ToolbarItems>
    

    <StackLayout x:Name="MainContentStack"> 
        <!-- Place new controls here -->
        <Grid x:Name="MainContentGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="7*"/>
            </Grid.RowDefinitions>
            <Grid x:Name="TopBar" BindingContext="{Binding CurrentTimeEntry}" Grid.Row="0" BackgroundColor="{StaticResource AbasGreen}" Padding="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Entry x:Name="CurrentTextLabel" Grid.Column="0" Grid.Row="0" Text="{Binding Comments, Mode=TwoWay}" BackgroundColor="Transparent"/>
                <views:AutoCompleteList x:Name="AutoCompleteList" Grid.Column="0" Grid.Row="1"/>
                <StackLayout Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Orientation="Horizontal" Padding="10,0,0,0">
                    <Label x:Name="TimerLabel" Text="00:00:00" HorizontalOptions="End" VerticalOptions="Center" FontSize="20"/>
                    <Button x:Name="TimerButton"  HorizontalOptions="EndAndExpand"  Text="{Binding TimerButtonText}" FontAttributes="Bold" TextColor="DarkSlateGray" CornerRadius="2" BorderWidth="5" BorderColor="DarkGray" BackgroundColor="{Binding TimerButtonColor}" Clicked="TimerButton_OnClicked" />
                </StackLayout>
               
            </Grid>
            <views:TimeEntryListView Grid.Row="1"/>
        </Grid>
    </StackLayout>

</ContentPage>
