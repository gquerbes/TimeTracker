<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TimeTracker"
             xmlns:views="clr-namespace:TimeTracker.Views;assembly=TimeTracker"
             x:Class="TimeTracker.MainPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="BUTTON" Clicked="MenuItem_OnClicked" />
    </ContentPage.ToolbarItems>
    

    <StackLayout x:Name="MainContentStack"> 
        <!-- Place new controls here -->
        <Grid x:Name="MainContentGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="7*"/>
            </Grid.RowDefinitions>
            <Grid x:Name="TopBar" BindingContext="{Binding CurrentTimeEntry}" Grid.Row="0" BackgroundColor="Beige" Padding="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!--<Entry x:Name="CurrentTextLabel" Grid.Column="0" Grid.Row="0" Text="{Binding Comments, Mode=TwoWay}" />-->
                <views:AutoCompleteList x:Name="AutoCompleteList" Grid.Column="0" Grid.Row="0" />
                <Label x:Name="TimerLabel" Text="0:00" Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"/>
                <Button x:Name="TimerButton" Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" HorizontalOptions="Center"  Text="{Binding TimerButtonText}" BackgroundColor="{Binding TimerButtonColor}" Clicked="TimerButton_OnClicked" />
            </Grid>
            <views:TimeEntryListView Grid.Row="1"/>
        </Grid>
    </StackLayout>

</ContentPage>
