<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TimeTracker"
             xmlns:views="clr-namespace:TimeTracker.Views;assembly=TimeTracker"
             x:Class="TimeTracker.MainPage"
             Title="abas Time Tracker ">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="BUTTON" Clicked="MenuItem_OnClicked" />
    </ContentPage.ToolbarItems>
    

        <!-- Place new controls here -->
        <Grid x:Name="MainContentGrid">
            <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid x:Name="TopBar" BindingContext="{Binding CurrentTimeEntry}" Grid.Row="0" BackgroundColor="{StaticResource AbasGreen}" Padding="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="125"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Editor x:Name="CurrentTextEditor" FontSize="15" Grid.Column="0" Grid.Row="0" Text="{Binding Comments, Mode=TwoWay}" BackgroundColor="Transparent" />
                <views:AutoCompleteList Opacity="1"  x:Name="AutoCompleteList" Grid.Column="0" Grid.Row="1" SelectedTicket="{Binding CurrentTimeEntry.Ticket}" />
                <StackLayout Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Orientation="Vertical" Padding="10,0,0,0" HorizontalOptions="End">
                    <Button x:Name="TimerButton"  HeightRequest="75"  WidthRequest="100" Text="{Binding TimerButtonText}" FontAttributes="Bold" TextColor="DarkSlateGray" CornerRadius="2" BorderWidth="5" BorderColor="DarkGray" BackgroundColor="{Binding TimerButtonColor}" Clicked="TimerButton_OnClicked" />
                    <Label x:Name="TimerLabel" Text="0:0:0" HorizontalOptions="Center" FontSize="15"/>
                <Switch IsToggled="{Binding Billed}" />

                </StackLayout>


        </Grid>
            <views:TimeEntryListView x:Name="EntryListView" Grid.Row="1"/>
        </Grid>

</ContentPage>
