<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TimeTracker.Views.TimeEntryListView">
  <ContentView.Content>
      <ListView Grid.Row="1" IsGroupingEnabled="True" ItemsSource="{Binding TimeEntries}" BackgroundColor="LightGray" SeparatorColor="{StaticResource AbasGreen}" SeparatorVisibility="Default"  CachingStrategy="RecycleElement" >
          <ListView.GroupHeaderTemplate>
              <DataTemplate>
                  <ViewCell>
                        <Grid BackgroundColor="{StaticResource AbasGray}" HeightRequest="35">
                          <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="*"/>
                              <ColumnDefinition Width="*"/>
                          </Grid.ColumnDefinitions>
                          <Label Grid.Column="0" Text="{Binding DateLabel}" TextColor="White" HorizontalOptions="Start"/>
                          <Label Grid.Column="1" Text="4:34"  HorizontalOptions="End" TextColor="White"/>
                      </Grid>
                  </ViewCell>
              </DataTemplate>
          </ListView.GroupHeaderTemplate>
          <ListView.ItemTemplate>
              <DataTemplate>
                  <ViewCell>
                      <Grid   BackgroundColor="White" Padding="5" HeightRequest="70">
                          <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="5*"/>
                              <ColumnDefinition Width="*"/>
                              <ColumnDefinition Width="Auto"/>
                          </Grid.ColumnDefinitions>
                          <Grid.RowDefinitions>
                              <RowDefinition Height="2*"/>
                              <RowDefinition Height="1*"/>
                          </Grid.RowDefinitions>
                          <Label  Grid.Column="0" Grid.Row="0" Text="{Binding Comments}" FontSize="Small" FontFamily=""/>
                          <Label Grid.Column="0" Grid.Row="1" Text="{Binding SelectedTicketLabel}" FontSize="Micro"/>
                          <Label  Grid.Column="1"  Grid.Row="0" Grid.RowSpan="2" Text="{Binding RunTimeText}"/>
                          <Button Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Text="continue" FontAttributes="Bold" TextColor="SlateGray" CornerRadius="2" BorderWidth="5" BorderColor="LightGray" BackgroundColor="{StaticResource AbasLimeGreen}" Clicked="PreviousJobTimer_OnClicked"/>
                      </Grid>
                      <ViewCell.ContextActions>
                            <MenuItem Text="Delete" Clicked="DeleteContextAction_Clicked" CommandParameter="{Binding .}" />
                      </ViewCell.ContextActions>
                  </ViewCell>
              </DataTemplate>
          </ListView.ItemTemplate>
      </ListView>
    </ContentView.Content>
</ContentView>