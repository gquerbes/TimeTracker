<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TimeTracker.Views.AutoCompleteList">
  <ContentView.Content>
      <StackLayout>
          <SearchBar Focused="SearchBarFocused" Unfocused="SearchBarUnfocused"/>
            <ListView IsVisible="{Binding IsListViewVisible}"  ItemsSource="{Binding Tickets}">
             <ListView.ItemTemplate>
                 <DataTemplate>
                     <ViewCell Height="80">
                         <Grid BackgroundColor="Red">
                             <Grid.ColumnDefinitions>
                                 <ColumnDefinition Width="*"/>
                                 <ColumnDefinition Width="*"/>
                             </Grid.ColumnDefinitions>
                             <Label Grid.Column="0" Text="{Binding key}"></Label>
                         </Grid>
                     </ViewCell>
                 </DataTemplate>
             </ListView.ItemTemplate>
          </ListView>
      </StackLayout>
  </ContentView.Content>
</ContentView>