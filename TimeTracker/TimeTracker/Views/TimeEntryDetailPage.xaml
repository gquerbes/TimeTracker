<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:TimeTracker.Views;assembly=TimeTracker"
             x:Class="TimeTracker.Views.TimeEntryDetailPage"
             BackgroundColor="LightGray">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" BackgroundColor="{StaticResource AbasGreen}" Padding="50">
           <Entry Text="{Binding Comments}"/>
            <views:AutoCompleteList SelectedTicket="{Binding Ticket}"/>  
            <Entry x:Name="TimeEntry" Text="{Binding RunTimeText, Mode=OneTime}" Unfocused="TimeEntry_OnUnfocused"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>